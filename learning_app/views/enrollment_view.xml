<!-- views/enrollment_views.xml -->
<odoo>
    <!-- list View -->
    <record id="view_enrollment_list" model="ir.ui.view">
        <field name="name">enrollment.list</field>
        <field name="model">school.enrollment</field>
        <field name="arch" type="xml">
            <list string="Enrollments">
                <field name="enrollment_number"/>
                <field name="student_id"/>
                <field name="course_id"/>
                <field name="enrollment_date"/>
                <field name="state" widget="badge" decoration-success="state == 'completed'"
                       decoration-danger="state == 'cancelled'" decoration-warning="state == 'pending'"/>
                <field name="final_grade"/>
                <field name="attendance_percentage"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_enrollment_form" model="ir.ui.view">
        <field name="name">enrollment.form</field>
        <field name="model">school.enrollment</field>
        <field name="arch" type="xml">
            <form string="Enrollment">
                <header>
                    <button name="action_confirm" type="object" string="Confirm"
                            states="draft" class="btn-primary"/>
                    <button name="action_complete" type="object" string="Mark as Completed"
                            states="confirmed" class="btn-success"/>
                    <button name="action_cancel" type="object" string="Cancel"
                            states="draft,confirmed" class="btn-danger"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,confirmed,completed,cancelled"/>
                </header>

                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="enrollment_number" readonly="1"/>
                        </h1>
                    </div>

                    <group>
                        <group string="Enrollment Details">
                            <field name="student_id" required="1"/>
                            <field name="course_id" required="1"/>
                            <field name="enrollment_date" required="1"/>
                            <field name="state"/>
                        </group>
                        <group string="Performance">
                            <field name="final_grade"/>
                            <field name="attendance_percentage"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Course Information">
                            <field name="course_id" mode="form">
                                <form>
                                    <group>
                                        <group>
                                            <field name="name"/>
                                            <field name="code"/>
                                            <field name="teacher_id"/>
                                            <field name="duration_hours"/>
                                        </group>
                                    </group>
                                </form>
                            </field>
                        </page>

                        <page string="Student Information">
                            <field name="student_id" mode="form">
                                <form>
                                    <group>
                                        <group>
                                            <field name="name"/>
                                            <field name="grade"/>
                                            <field name="age"/>
                                            <field name="phone_number"/>
                                        </group>
                                    </group>
                                </form>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_enrollment_search" model="ir.ui.view">
        <field name="name">enrollment.search</field>
        <field name="model">school.enrollment</field>
        <field name="arch" type="xml">
            <search>
                <field name="enrollment_number"/>
                <field name="student_id"/>
                <field name="course_id"/>

                <filter name="draft_enrollments" string="Draft" domain="[('state', '=', 'draft')]"/>
                <filter name="confirmed_enrollments" string="Confirmed" domain="[('state', '=', 'confirmed')]"/>
                <filter name="completed_enrollments" string="Completed" domain="[('state', '=', 'completed')]"/>

                <separator/>

                <group expand="0" string="Group By">
                    <filter name="group_by_course" string="Course" context="{'group_by': 'course_id'}"/>
                    <filter name="group_by_student" string="Student" context="{'group_by': 'student_id'}"/>
                    <filter name="group_by_state" string="Status" context="{'group_by': 'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_enrollment" model="ir.actions.act_window">
        <field name="name">Enrollments</field>
        <field name="res_model">school.enrollment</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_enrollment_search"/>
        <field name="context">{'default_state': 'draft', 'default_enrollment_date': current_date}</field>
    </record>
</odoo>